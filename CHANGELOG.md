# Changelog

## 1.5.2 - 2026-09-05

- 同步上游 phyllis-0612/st-image-atelier 1.5.0：主题模式、画廊按天数/张数自动保留清理、画师串独立负面串、重新生成状态优先级等全部并入。上游的画廊保留规则取代本 fork 之前的 `galleryKeepMax` 客户端裁剪。
- 修复点开小铅笔改写再关上之后提示词原文露在楼里、图卡掉到楼底的问题。酒馆重建这一层时会把提示词当 Markdown 渲染：`1.` 列表序号、`**加粗**` 星号等被吃掉，`<draw>` 被消毒器剥掉，按字面比对永远对不上。现在比对时两侧都去掉空白与 Markdown 语法字符，再不行就用提示词头尾锚点定位，整段替换成卡片并清掉空的列表壳。
- 楼底 fallback 里的卡片每轮都会尝试接回原地；`MESSAGE_EDITED` / `MESSAGE_UPDATED` 改为等 DOM 落定后再处理；小铅笔打开期间不动这一层。
- 恢复并扩充改写重挂载回归测试：Markdown 列表 / 加粗、楼底自愈、宏替换模糊定位、正文引用提示词片段不误删、改写中不塞卡片。

## 1.5.0 - 2026-09-04

- 新增日间、夜间、跟随酒馆三种主题模式，设置面板、消息卡片、画廊和原图查看层同步切换。
- 为文本框、下拉框和多行输入框增加独立前景色、背景色、占位符色与原生控件配色，日夜模式输入文字均满足 WCAG AA 对比度。
- 新增画廊按自定义保留天数、保留张数自动清理；两条规则可单独或同时启用，采用任一命中并从最旧图片开始删除。
- 清理在扩展启动、生成成功、打开画廊、手动保存规则后及每小时维护时检查；并发检查自动合并，避免重复删除。
- 直连模式和可选 Server Plugin 增强模式均同步图片文件、画廊墓碑和标签结果状态；只处理 Image Atelier 登记的图片。
- 新增规则选择、迁移、并发清理、文件与消息元数据同步、主题应用及输入框对比度测试。

## 1.4.4 - 2026-09-02

- 将画师串 JSON 导入/导出入口移动到预设选择器正下方，并增加明确的“预设分享（JSON）”标签。
- 强调每套画师预设独立保存名称、正面串和负面串；原“固定负面提示词”改名为可选的全局附加负面提示词，避免与逐预设负面串混淆。
- 修复已有图片重新生成时旧图遮住任务状态的问题；点击“重新生成”后立即显示“正在重新生成”与加载动画。
- 自动清理 NAI 地址和生图路径中的零宽空格等不可见字符，避免复制地址后生成错误请求路径。
- 新增重新生成状态优先级和不可见字符地址归一化回归测试。

## 1.4.3 - 2026-09-01

- 修复多行 `<draw>` 提示词在编辑消息后与旧生图卡片同时露出的问题；渲染器会还原 `<br>` 和块元素换行，并清理已有卡片对应的重复标签正文。
- 编辑消息时同步移除已经不存在的旧标签卡片，保留仍存在标签的卡片与生成记录。
- 画师串预设新增“导出当前”“导出全部”和“导入 JSON”，可分享正面与负面画师串。
- 分享文件不包含预设 ID、时间戳、API Key 或 NAI Token；导入会校验格式、跳过完全重复项，并为内容不同的同名预设自动改名。
- 新增多行 DOM 渲染、分享格式、密钥隔离、重复导入与重名导入回归测试。

## 1.4.2 - 2026-09-01

- 修复 Aurora 中转站 NAI 生图：`/api` 与 `/api/v1` 自动使用真实的 `/api/generate-direct` 端点。
- 支持 `application/x-ndjson` 流式响应，识别排队、生成、失败与成功事件，并将 `/img/...` 相对地址补全为中转站绝对地址后保存。
- 画师串预设新增独立负面串；旧预设自动迁移为空负面，正面内容不变。
- 正面按「画师串 → 正文 → 质量标签」组合，负面按「画师负面串 → 固定负面提示词」组合。
- 新增 Aurora 流式响应、端点归一化、相对地址和正负画师串回归测试。

## 1.3.7 - 2026-08-01

- 将正文卡片的“下载”和画廊下载流程改为酒馆内原图查看层，避免 iOS、安卓 WebView 和内置浏览器跳入无法返回的文件预览页。
- 原图使用真实同源 `<img>` 展示并显式恢复系统长按菜单；正文图片、画廊缩略图和原图均支持长按保存。
- 原图查看层提供始终可见的右上角关闭按钮、底部关闭按钮、背景关闭、Esc 和手机返回键关闭，并恢复聊天滚动环境。
- 支持文件分享的系统额外提供“分享 / 保存”入口；不支持时继续提示长按保存。
- Server Plugin 原图响应明确使用 `Content-Disposition: inline`，附件下载端点仅保留为兼容能力。
- 新增关闭按钮、浏览历史返回和长按事件不被拦截的回归测试。

## 1.3.6 - 2026-07-31

- 恢复 `512x768` 常用竖图尺寸。
- 将默认尺寸下拉框扩充为 23 个选项，覆盖常用方图、1:2、2:3、3:4、9:16 竖图及对应横图尺寸。
- 所有实际尺寸值继续使用接口兼容的英文小写 `x`，并在界面提示不同上游模型的尺寸支持范围可能不同。
- 新增尺寸选项完整性、格式和横竖配对回归测试。

## 1.3.5 - 2026-07-31

- 修复唯一一张自动生图也被错误显示为“自动排队中”的状态语义问题。
- 当前开始执行的任务会立即显示“正在生成”，不再为直连请求写入没有实际意义的 `queued` 中间态。
- 保留真正队列等待状态的卡片样式，并新增直连任务不得经过虚假排队状态的回归测试。

## 1.3.4 - 2026-07-31

- 修复自动生图首次保存幂等记录时，酒馆消息重绘触发解析并将当前任务误判为 `interrupted` 的竞态。
- 在首次聊天保存前登记活跃任务；每次状态落盘前重新获取当前标签对象，兼容消息重绘时元数据对象被替换。
- 使用聊天 DOM 突变触发并在最后一次变化后短防抖处理，捕捉正文完成后瞬间注入的生图标签。
- 将全量消息来源扫描降为 1.5 秒低频兜底，不再承担主要实时识别工作。
- 自动队列对排队中和生成中的同一标签统一去重，避免重绘期间重复入队。
- 新增“保存即重绘”自动生图竞态和运行中队列去重回归测试。

## 1.3.3 - 2026-07-31

- 在聊天输入框左下角的魔法棒扩展菜单中增加「Image Atelier」快捷入口。
- 修复旧入口因选择器命中 `#extensions_settings` 而被错误插入扩展设置页的问题。
- 菜单稍晚加载时通过 DOM 监听自动补挂载，兼容 SillyTavern 1.14–1.18 的加载时序。
- 入口采用酒馆原生菜单项结构与魔法棒图标，支持点击、Enter 和空格键打开现有插件面板。

## 1.3.2 - 2026-07-31

- 兼容 SillyTavern 消息净化器直接移除 `<draw>` 开闭标签、仅保留提示词正文的情况：按规范化后的提示词内容定位并在原处替换卡片。
- 提示词跨换行或包含连续空白时仍可准确匹配；无法可靠定位时才保留末尾兜底卡片。
- 上游返回 moderation/content rejected 时显示内容审核专用提示，不再误导用户关闭 size、quality 或 n。
- 新增标签被过滤后的原位定位与审核拒绝错误回归测试。

## 1.3.1 - 2026-07-31

- 将转义后显示在正文里的 `<draw>...</draw>` 文本范围原位替换为生图卡片，不再默认追加到消息末尾。
- 支持同一消息中多个标签、正文前后内容，以及已有卡片后再次追加的新标签。
- 同时订阅酒馆提供的全部消息更新/编辑事件，不再只取第一个可用事件。
- 新增聊天 DOM 变化监听，并以低频文本变更扫描兜底，实时发现正文完成后延迟插入的标签。
- 延迟追加标签按实时消息处理，可正常进入自动生图队列；稳定 Tag ID 继续防止重复插卡与重复提交。

## 1.3.0 - 2026-07-31

- 按新版设计稿重构设置页、消息内五态生图卡片与双列画廊。
- 在新版 API 配置区融合命名预设的选择、新建、删除和独立密钥管理。
- 默认尺寸改为带友好显示标签的下拉框，实际值始终保留英文小写 `x`。
- 发送请求前统一修正 `×`、`X`、`*` 等尺寸分隔符，兼容严格校验的上游接口。
- 同时保护比例映射和额外 JSON 参数中的 `size`，避免相同错误再次出现。
- 完成 390×844 手机视口与桌面视口视觉检查，无横向溢出。

## 1.2.0 - 2026-07-31

- 将 URL、API Key 和已选模型保存为可命名的多 API 预设，支持下拉快速切换。
- 每个预设使用独立密钥存储，并自动迁移 1.1.0 的旧单预设与密钥。
- 将模型改为真实下拉框，并把「拉取模型」按钮移动到模型选择旁边。
- 拉取模型与模型接口测试会先保存当前表单，不再要求用户提前另点一次保存。
- 生图失败会展示脱敏后的上游错误详情、HTTP 状态和常见参数/路径提示。
- 明确区分无扣费的模型接口测试与实际生图接口验证。
- 加固可选 Server Plugin 的 Windows 原子写入与元数据事务，避免完成状态早于落盘。

## 1.1.0 - 2026-07-31

- 默认改为免服务端直连模式，可从 SillyTavern 扩展窗口粘贴仓库链接安装。
- 使用酒馆内置 `/api/images/upload` 与 `/api/images/delete` 保存和删除图片。
- attempt、result 与删除抑制状态迁移到 `message.extra.stImageAtelier`。
- 画廊索引迁移到用户扩展设置，API Key 使用账户隔离的前端存储。
- 添加 CORS 专用错误诊断和直连模式自动化测试。
- Server Plugin 改为显式可选的增强模式，普通安装不再修改 `config.yaml`。

## 1.0.0 - 2026-07-31

- 完成 `<draw>` 多标签解析、稳定 Tag ID 与消息内卡片。
- 完成单 API 预设、模型拉取、连接测试和密钥隔离。
- 完成服务端幂等、URL/Base64 图片保存与重启中断恢复。
- 完成简化自动生图串行队列与每消息最多三个标签限制。
- 完成基础画廊、原图下载、删除墓碑和移动端主题适配。
- 添加安装、验证、卸载脚本和 mock 全链路测试。
